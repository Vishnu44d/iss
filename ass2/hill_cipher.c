#include<stdio.h>
#include<string.h>
#include<stdlib.h>

#define BUFF_SIZE 1024

int Key = {
    {1, 2, 3},
    {5, 8, 9},
    {12, 25, 21}
    }

char *encrypt(char *palin_text)
{
    int i=0;
    char *cipher_text = (char*)malloc(BUFF_SIZE);

    int mat[100][3];
    int j, k, m=strlen(palin_text)/3;
    for(i=0; i<strlen())

    while(palin_text[i]!='\0'){
        
        i++;
    }
    
    return cipher_text;
}